'use client';

import { Input } from '@/app/components/inputs';
import { Body } from '@/app/components/modals/registerModal/styles';

export const RegisterBody = (
  <Body>
    <Input id={} label={} register={} errors={} />
  </Body>
);
